import { AdminPageCard } from "../_components/admin-page-card";

export default function AdminUsersPage() {
  return (
    <AdminPageCard
      title="Usuarios"
      description="Consulta usuarios, roles y acceso administrativo de la plataforma."
    />
  );
}
